# Distribute outside the Mac App Store

Just Notes will use direct macOS distribution instead of the Mac App Store so it can integrate with local tools, including installing read-only Agent Guides for Codex and Claude in their user-level directories. Releases should remain signed and notarized, but preserving App Sandbox compatibility is no longer a product constraint; this trades Store-managed distribution and sandbox isolation for a lower-friction local integration experience.
