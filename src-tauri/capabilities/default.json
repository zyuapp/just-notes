{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the Just Notes windows",
  "windows": ["main", "indicator"],
  "permissions": ["core:event:default", "core:window:allow-start-dragging"]
}
